{
  "name": "earth-watch",
  "version": "1.0.0",
  "description": "Earth Watch — space weather, disasters, seismic, live cams",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint": "node --check server.js && for f in lib/*.js public/js/*.js public/js/**/*.js; do node --check \"$f\" || exit 1; done",
    "build": "echo 'no build step required'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
